---
import Layout from '~/layouts/Layout.astro'
import { createWebPageSchema } from '~/utils/schema'
import { getTranslations as getI18nTranslations } from '~/i18n/utils'

const lang = 'en' as const
const t = getI18nTranslations(lang)
const pageSchema = createWebPageSchema({
  name: t.pages.contactPage.heroTitle,
  url: Astro.url,
  description: t.pages.contactPage.heroSubtitle,
})
---

<Layout
  title={`${t.pages.contactPage.heroTitle} – LAOM`}
  description={t.pages.contactPage.heroSubtitle}
  seo={{
    ldJSON: pageSchema,
  }}
>
  <section class="py-32 lg:py-40 bg-white">
    <div class="max-w-5xl mx-auto px-8 sm:px-12 lg:px-20 text-center">
      <span class="block text-xs font-light tracking-[0.2em] uppercase text-black/40 mb-6">
        Contact
      </span>
      <h1 class="text-5xl md:text-6xl lg:text-7xl font-light text-black mb-6 tracking-tight">
        {t.pages.contactPage.heroTitle}
      </h1>
      <p class="text-xl md:text-2xl font-light text-black/60 max-w-3xl mx-auto leading-relaxed">
        {t.pages.contactPage.heroSubtitle}
      </p>
    </div>
  </section>

  <section class="py-24 bg-[#1a1a1a] text-white">
    <div class="max-w-4xl mx-auto px-8 sm:px-12 lg:px-20 text-center">
      <p class="text-xs tracking-widest uppercase text-white/40 mb-4">{t.contact.development}</p>
      <p class="text-2xl md:text-3xl font-light mb-8">{t.contact.name}</p>
      <div class="space-y-4">
        <a href={`mailto:${t.contact.email}`} class="block text-xl font-light hover:text-white/70 transition-colors">
          {t.contact.email}
        </a>
        <a href={`tel:${t.contact.phone.replace(/\s/g, '')}`} class="block text-xl font-light hover:text-white/70 transition-colors">
          {t.contact.phone}
        </a>
      </div>
    </div>
  </section>
</Layout>
